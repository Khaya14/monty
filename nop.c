#include "monty.h"

/**
 * f_nop - code that does nothing
 * 
 * @head: the stack head
 * @count: the line number
 * Return: no return
 */

void f_nop(stack_t **head, unsigned int count)
{
	(void) count;
	(void) head;
}
